# Calculator

A simple, elegant web-based calculator built with HTML, CSS, and JavaScript.

## Features

- Basic arithmetic operations (addition, subtraction, multiplication, division)
- Clean and modern user interface
- Keyboard support for faster input
- Responsive design
- Error handling for invalid operations

## Usage

### Opening the Calculator

Simply open `index.html` in your web browser. No installation or build process required.

### Using the Calculator

**Mouse/Touch Input:**
- Click on number buttons (0-9) to enter numbers
- Click on operator buttons (+, -, *, /) to perform operations
- Click '=' to calculate the result
- Click 'C' to clear the display
- Click '←' to delete the last character

**Keyboard Input:**
- `0-9`: Enter numbers
- `+`, `-`, `*`, `/`: Enter operators
- `Enter` or `=`: Calculate result
- `Escape` or `C`: Clear display
- `Backspace`: Delete last character

## File Structure

```
calculator/
├── index.html    # Main HTML structure
├── styles.css    # Styling and layout
├── script.js     # Calculator logic
└── README.md     # Documentation
```

## Technical Details

- **HTML**: Semantic structure with a display area and button grid
- **CSS**: Modern styling with gradient background and hover effects
- **JavaScript**: Event-driven calculator logic with keyboard support

## Browser Compatibility

Works on all modern browsers including:
- Chrome
- Firefox
- Safari
- Edge

## License

This project is open source and available for use.
